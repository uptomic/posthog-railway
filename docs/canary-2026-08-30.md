# Canary and restore rehearsal: 2026-08-30

This is a chronological evidence log. Earlier pending states and superseded deployments are
retained as history; the latest acceptance status is recorded at the end. A successful
migration or image build alone does not establish completed production acceptance.

**Final status: production migration and acceptance completed on 2026-08-30.**
The final immutable bundle is recorded by manifest commit `f66eb985eb31142aa5f999a10620ffee196613a9`.
All 22 release roles match it; capture, replay, flags, MCP, the original six-report
dashboard and Pulse's production reader passed their separate acceptance checks.
The PostHog-only Cloudflare challenge was subsequently disabled at the user's request;
DNS, TLS, proxy settings and application authentication remain intact. Historical
Pulse instrumentation limitations remain explicitly reported, not backfilled or fabricated.

## Initial fresh-schema candidate

- Upstream PostHog commit: `d29386663d1c52625552dbb8c47754851dd9e425`
- Official `posthog` image: `ghcr.io/posthog/posthog@sha256:ba39b41c8dcf723a0f33f20f38bca3946a6baa1c065bea960a55dbc28113fed6`
- Official `posthog-node` image: `ghcr.io/posthog/posthog-node@sha256:e526ec11a5fe7cb1d60037a5977eb045c3fec13595f87b4eea01db96dfff076c`
- Uptomic MCP image: `ghcr.io/uptomic/posthog-railway/mcp@sha256:143642c362fe4d85f26d56d4b81c310f8fe6b32a1bc7df196641dcc9eecf25e2`

## Initial fresh-schema result

The isolated local canary started successfully on fresh PostgreSQL, ClickHouse, Kafka/Redpanda,
Redis, and object-storage volumes.

- Embedded Web revision matched the candidate commit exactly.
- Django reported zero pending migrations after startup.
- ClickHouse recorded 315 migrations through `0309_metrics_kafka_ingest` and reported that its
  migrations were up to date.
- `GET /_health` returned HTTP 200 with body `ok`.
- Unauthenticated `GET /api/` returned HTTP 401, confirming that the application route was live and
  protected.
- The Web container remained running after the migration and server-start sequence.

The first boot exposed the expected dependency ordering: ClickHouse migrations could not run until
Django had created its metadata tables. After Django completed, the coordinated second startup
applied all ClickHouse migrations and reached readiness. Production must preserve this migration
order.

## Scope of the initial canary

The initial canary proved clean installation and startup only. The restored-data rehearsal below
provides separate upgrade evidence for the later locked candidate. Neither result establishes
production completion; production verification from the promotion runbook remains required.

## Restore-rehearsal migration finding

The later restore rehearsal for locked candidate
`da93d8bbd71e23d49a8372ec43b4a4c7f75c1316` found that a direct jump from the inherited
pre-0210 ClickHouse ledger is unsafe. Current source causes historical replay migrations to refer to
columns introduced later in the same chain.

The release lock therefore includes two exact predecessor images. On the disposable restored
ClickHouse volume, Migration Bridge 1 and Migration Bridge 2 each completed successfully in order.
The final migrator then completed successfully with the locked latest-release image, leaving 315
entries in the ClickHouse migration ledger. The successful restore-migrator service was
`edffccec-84c9-4694-8bca-25680d59a6d6`, deployment
`a7a28a50-9dcf-4921-bfe5-0de411a37c33`. Bridge success alone is not the migration gate; the final
release migrator must also finish successfully.

The same candidate replaces the retired standalone Cyclotron janitor with the official Node image in
`cdp-cyclotron-v2-janitor` mode. Its isolated `cyclotron_node` database is migrated first with the
locked `sqlx-migrate` image and the explicit command
`/migrations/bin/migrate-entry cyclotron-node`. Set `CYCLOTRON_NODE_DATABASE_URL` on that one-shot
service; `DATABASE_URL` alone is ignored by this entrypoint. PersonHog Replica and Router are
required upstream of the Node modes. Replay also requires `recordings-blob-ingestion-v2` and
`recording-api`; replay capture and Kafka alone do not write the replay events table.

Feature Flags uses the same `SECRET_KEY` as Web. The service starts without it, but logs that
encrypted remote configuration cannot be decrypted, so that warning is a failed canary.

Web and Worker must have `RECORDING_API_URL` plus a matching `INTERNAL_API_SECRET`, and must enable
PersonHog with the Router address. The restored-data replay canary passed only after this complete
topology was present.

## Restored-data rehearsal results

The rehearsal for `da93d8bbd71e23d49a8372ec43b4a4c7f75c1316` passed:

- authenticated PostHog UI access;
- session-replay metadata access;
- bounded HogQL checks finding one synthetic capture event and one synthetic replay event;
- MCP initialization, `project-get`, and a bounded `query-trends` aggregate returning 2,196.

These are isolated rehearsal results, not production acceptance. The later production routing
checks are recorded separately below; final end-to-end verification remains pending.

## Public SDK routing finding

The official Web image returned HTTP 302 for rehearsal requests to `/e` and `/s`; it does not replace
the public ingress routing previously supplied by the inherited template. The release now owns that
routing in `config/gateway.Caddyfile`, with the existing `Capture` service retained as the gateway
and a separate `PostHog-Capture-Backend` running the official capture image. See the
[gateway contract](gateway.md) for private URLs, domain cutover, and the required public-path checks.
This finding does not change the completed isolated capture/query checks above, and does not claim
that the production domain cutover is complete.

The exact locked Caddy image validated the shared configuration and passed the local
`gateway:smoke` check: 16 HTTP routes, HTTPS forwarding, Web fallback, and livestream prefix
stripping. This is configuration/routing proof with synthetic local upstreams, not production
ingestion proof.

The original status-only health probe could mistake a proxied Web HTTP 200 for gateway health.
The corrected config uses a first `handle /health` block. The strengthened smoke check also passed
with Web deliberately unavailable: `/health` returned an empty HTTP 200 while `/` returned 502.

## Production routing checks: 2026-08-30

The cutover operator subsequently verified the canonical domain on the gateway and its preserved
Railway capture hostname. Each host passed health, analytics capture, replay capture, exact HogQL
canary counts, replay metadata, and flag evaluation. See the [gateway evidence](gateway.md#domain-cutover-and-verification)
for the active domain mapping and Cloudflare challenge exclusions.

Recording API also requires `SESSION_RECORDING_API_REDIS_HOST` to reference its own working full
authenticated `REDIS_URL`. A bare private hostname failed because the locked ioredis client defaults
to IPv4 while inherited private DNS is IPv6-only; `?family=6` is an invalid string-valued substitute
for a numeric client option. This correction retains the same official Node image; the
[private-network contract](private-networking.md#recording-api-redis-client) owns the requirement.

Production authenticated UI browser acceptance is still pending. Canonical application APIs without
Cloudflare clearance are intentionally challenged; successful direct Web/API checks do not replace
that browser verification. These observations do not claim full production migration completion.

## Production migration and integration receipt

The final production migrator deployment `bb1de200-f6e8-4603-887e-af3328565a66` emitted
`All migrations completed successfully.` after both ordered bridge migrations. The resulting
PostgreSQL ledger contains 2,615 migrations and ClickHouse contains 315 ledger entries. The
persons migration pass applied 11 migrations and intentionally skipped its three partition
migrations; the asynchronous-migration gate passed. The isolated Cyclotron database completed
all 14 SQLx migrations before its consumers started.

The production Recording API deployment `a545daa3-8f5d-4704-87b9-4f3c616db2be` is healthy on
the same locked official Node image. Its authenticated Redis PING and `/_health` check passed.
Both synthetic replay canaries subsequently returned their actual recording blobs: HTTP 200,
`application/jsonl`, 1,148 bytes and five valid rrweb snapshots each (metadata, full DOM, and
three interactions). This proves stored recording retrieval, not browser playback.

The production MCP protocol `2025-06-18` passed initialization, default `exec`-mode
`project-get` for project 1, and `query-trends` returning the exact two-event synthetic canary
aggregate. Pulse's unchanged typed client also passed its full prior-day query set with its
10-second per-request ceiling unchanged. The August 29 identified-navigation aggregates were
zero; no substitute or fabricated metric was introduced.

The existing Infisical production sync successfully propagated the validated MCP URL,
project identifier, authorization header, and `required` source mode to the Pulse sync worker
at `2026-08-30T16:47:05.567Z`. In-memory equality checks passed for all four settings, and
the destination-configuration typed read passed. Secrets remain restricted to the sync worker.

The first real scheduled import exposed a separate immutable-report conflict: August 29 had
already been finalized with 17 metrics while the PostHog source was disabled, and it contains
no imported PostHog aggregate rows. The import failed closed rather than rewriting that
published snapshot. That initial failure did not establish a completed Pulse import. The later
production fix and successful import receipts are recorded below; authenticated browser
acceptance remains open.

Production PostgreSQL HA was rechecked with postgres-2 as leader and postgres-1/3 streaming
at zero lag. All three credential-equality checks passed. The temporary direct database TCP
proxy was removed; rollback data and named backup anchors remain retained.

### Retained rollback anchors and cleanup

The following production volume backups were re-queried after cutover and still exist:

| Store | Backup ID | Created (UTC) |
| --- | --- | --- |
| ClickHouse | `7849a346-648b-434f-8138-5bbd4839b2d9` | 2026-08-30 08:14:58 |
| Kafka | `b797ca0a-f2fa-40a6-a084-ad746874945c` | 2026-08-30 13:11:14 |
| ZooKeeper | `f409deed-2b60-419e-bb1d-4036ab2d9d20` | 2026-08-30 13:11:15 |

The PostgreSQL PITR drill and restored rehearsal data remain retained. The temporary rehearsal
PAT was matched by metadata and credential hash, revoked only in the rehearsal environment,
and verified to return HTTP 401 before its local temporary file was removed.

All 18 approved application/one-shot deployments in the isolated rehearsal environment were
removed after production capture, query, flags, replay-blob and MCP proof. Its Redis, Valkey,
Temporal, Kafka, ZooKeeper, ClickHouse and PostgreSQL services remain healthy; all four volume
instances are retained. No production service, database, volume or backup was deleted by this
cleanup. The stopped rehearsal applications can be redeployed against the retained data.

### Database proxy latency repair

The intermittent MCP timeout was traced to one unreachable PostgreSQL HAProxy replica that
Railway still listed as running and advertised in private DNS. The other two proxies returned
writable SQL in approximately 50–65 ms. The unreachable address left Celery connections in
`SYN_SENT` for about 130 seconds and accumulated a worker backlog; credentials and CPU/memory
pressure were not the cause.

A controlled proxy-only replacement pinned the unchanged artifact
`ghcr.io/railwayapp-templates/postgres-ha/haproxy@sha256:d4143116bd4e7ac1db36b08c9148af9d911a286b171655449800749969dc68b0`.
It retained three EU replicas and the public TCP target of 5432, with `PORT=8404`, native
`/stats` process health, 120 seconds of overlap and 30 seconds of draining. Native stats are
not a database-readiness claim: every replacement separately passed writable SQL and showed
one healthy primary plus two healthy replicas during overlap.

Replacement deployment `1e7e4faa-d498-4b4f-9994-33854b18a893` is successful; old deployment
`ec124035-8fe3-49a4-bd54-13bd2579c725` and all its instances are removed. Private DNS now
contains exactly the three new reachable addresses, each passing writable SQL in 49–68 ms.
The worker backlog, unacknowledged messages and stuck connection count returned to zero without
a worker restart. Pulse's unchanged authenticated MCP read then passed all four stages in
114, 463, 387 and 346 ms. No PostgreSQL restart or application credential change was needed.

### Pulse production import and immutable retry

Pulse implementation commit `d0ba79ef442d57e05b7b8ac942fa026f5d55b742` permits a complete
first late source-only import behind a report whose six PostHog metrics were all published
as `not_instrumented`. It does not rewrite that report. An identical retry retains every
aggregate row ID, source timestamp, run ID and query-template hash; partial, changed or
incoherent stored evidence still fails closed. Scalar comparisons follow PostgreSQL's
eight-decimal, ties-away-from-zero storage semantics. The ADR lives in Pulse at
`decisions/2026-08-30-late-posthog-source-import.md`.

Production worker deployment `808776f7-83dd-4489-9f32-6db05490fc04` runs exact Pulse SHA
`bb256b3519a8dbaddef41b01c705c2a3054a8d0d`, which also contains an exact secret-scan
false-positive classification for the public rehearsal environment identifier. The scanner
exception is restricted to detector, file and value hash; changed values, paths or detectors
remain blocked. The final plain quality gate passed 494 tests. Focused tests passed 39 cases,
including 13 real PostgreSQL cases, and existing PostgreSQL store/materializer tests passed
1 and 7 cases. Independent review passed. GitHub quality run `33325166967` and secret scan
`33325166951` both completed successfully at this exact SHA.

The operator started and verified two real production sync runs:

| Run | Completion (UTC) | Result |
| --- | --- | --- |
| `01a053b4-3c62-7296-a110-e63f3fba776a` | 2026-08-30 17:25:21 | succeeded, 18 first-import rows |
| `01a053b4-ddfe-7c9c-9d45-aecbe528f0bd` | 2026-08-30 17:26:01 | succeeded, identical no-write aggregate retry |
| `01a053d6-28eb-70be-9ae8-b033d4155e39` | 2026-08-30 18:02:25 | succeeded, automatic hourly retry; both hashes unchanged |

Both used nine bounded Career Mentor export queries, two source connections and zero timeouts.
The original import quality proof is `passed`, expected/actual 18, nonblocking, with limitation
`POSTHOG_SOURCE_ONLY_AFTER_FINALIZATION`. Canonical row hashing before and after proved:

- The complete August 29 report, 17 metric rows and digest retained SHA-256
  `1fa533581cb5841ce54dfbd531cfe5f91a02545198a503f5b5666ecbe52194ad`.
- All 18 full aggregate rows retained SHA-256
  `88874b85d00525de0d3a3e741bd9b2ddc59e7f05687ceb673641f32feb67173c`
  across the retry, including their original metadata.

The existing August 29 PostHog metrics remain truthfully `not_instrumented`; the appended
source rows are not retroactively part of that published report. PostHog-backed reporting
begins with a subsequent real completed UTC report day. Both successful syncs also emitted
`SOURCE_FRESHNESS_SLO_BREACHED`: the source reconciliation was about 17.3 hours old. No
timestamp was refreshed or warning suppressed to conceal that limitation.

GitHub's existing automatic deployment trigger also refreshed Pulse's unchanged API, Web,
email scheduler and migrator at this SHA. The duplicate queued worker deployment was cancelled
in favor of the explicitly triggered exact-SHA worker. Public Pulse health returned 200 and
unauthenticated `/v1/access` returned the expected 401; PostHog gateway health returned 200.

Final acceptance remains incomplete: the canonical authenticated PostHog UI and actual browser
playback still need verification. The initial Cloudflare/old-524 observation was not a complete
diagnosis. Subsequent anonymous `/login` checks also timed out directly against both Web
replicas, despite their fast root-to-login redirects. Backend, recording-blob and MCP proofs
must not be presented as authenticated browser proof.

### HTML bootstrap dependency finding

Both gateways reached both Web replicas successfully, and every Web replica passed bounded
PostgreSQL checks without active database blockers. The root's anonymous 302 response does
not render HTML. The locked Web source renders `/login` through `preflight_check`, which
requests the private CDP API `/_health` without a timeout.

The Plugins service was correctly configured in `cdp-api` mode on port 6738, but its only
instance had exited while Railway still reported the deployment as successful. Its CDP
shadow Valkey pool had exceeded the shared Redis client's error retry limit and terminated
the process. This left the Web HTML dependency request waiting for an unavailable service.

The exact deployed Node image contains ioredis 4.28.5. Valkey's private hostname has an AAAA
record and no A record. Bounded probes against that same private Valkey returned `ENOTFOUND`
with the default client and `PONG` with numeric `family: 0` or `family: 6`. A string-valued
URL query parameter is not an equivalent fix. Valkey has no public endpoint and remains
private. Its CDP host settings must not contain credentials because the locked source logs
those settings verbatim; the separate password option owns authentication when required.

The corrective release must preserve the pinned upstream Node application, provide a tested
private-host-only dual-stack compatibility layer, and prove sustained CDP process health
after asynchronous Redis checks. Fast `/login` HTML and `/_preflight` from both Web replicas
are required before retrying canonical browser acceptance. An initial health response or a
successful Railway deployment alone cannot satisfy this gate.

The compatibility implementation was reviewed and pushed in release commit
`e1b811d24c4dfdd2d372441ff0574ca1fbd4d360`. Its 31 tests and 179 assertions passed,
along with lock validation, JSON validation and parsing of the workflow's nine shell scripts.
The exact deployed ioredis 4.28.5 also passed a nonpersistent helper probe against private
Valkey: default `family: 0` and preserved explicit `family: 6` returned `PONG`, while
preserved explicit `family: 4` returned the expected `ENOTFOUND`. The compiled-constructor
guard was rechecked against the running official image. No runtime files or configuration
were changed by that helper probe.

[Candidate build 33326926258](https://github.com/uptomic/posthog-railway/actions/runs/33326926258)
was dispatched with `update_upstream=false`; this retains the validated upstream release
while building and testing the compatibility image. These source and helper checks are not
yet proof of a completed image build or production correction.

That run built Node digest
`sha256:646fbdbda1f444040df0521e759cd6711916c4b970bbd0c6312e722b1aaf42e1` and passed
MCP/ClickHouse verification. Anonymous registry access passed for the Node manifest, image
configuration and all 31 layers; its upstream revision and overlay fingerprint matched.
Node image verification then stopped before the Redis protocol tests: the disposable fixture
used an ARM64-only Redis digest on an amd64 runner. Commit `f083e42` corrected the fixture
to a verified multi-platform index. Run `33327342008` then exposed a second fixture issue:
glibc maps a `::1` hosts-file alias to `127.0.0.1` for an IPv4 lookup. Commit `ad939ec`
uses a real non-loopback private IPv6 address on an isolated dual-stack Docker network.

Run `33327775853` reached the DNS assertion but returned `EAI_AGAIN`, not `ENOTFOUND`,
from Docker's isolated resolver. Commit `5b48ae8` accepts only those two lookup failures,
with the exact fixture hostname and `getaddrinfo` syscall still required. All baseline
explicit-IPv6 and overlay IPv6, IPv4, unrelated-host and explicit-family PONG checks remain
mandatory. The independent review passed; all 33 release tests and 195 assertions plus
authenticated lock verification passed. These fixture changes do not change the application
image digest or overlay fingerprint, and none of the failed runs authorized promotion.

[Run 33328207526](https://github.com/uptomic/posthog-railway/actions/runs/33328207526)
completed successfully at source `5b48ae8a66d4a72c75406311007b09220a786d8c`. The exact
official image reproduced the default-client IPv6 lookup failure while explicit IPv6
returned PONG. The exact overlay returned PONG for private IPv6, private IPv4, an unrelated
IPv4 host, explicit IPv4 and explicit IPv6; inherited startup metadata and runtime provenance
also passed. ClickHouse and MCP verification passed before the finalizer recorded the
candidate in commit `805b13e`. Local candidate verification and plan rendering subsequently
passed with all five Node roles referencing the verified `646fbdbd` digest.

The signed-in Pulse browser path was independently checked after the automatic hourly sync.
Its August 29 immutable snapshot rendered all 17 governed metrics: four available and 13
limited. The previously published PostHog limitations remain visible, consistent with the
unchanged report hash and the explicitly source-only late import.

### Sustained CDP recovery and authenticated browser verification

Production Plugins deployment `5f536188-4588-439d-a87e-01fadf363567` uses the verified Node
digest. Its scoped environment patch changed only the image, credential-free private Valkey
endpoint URL and restart policy (`ALWAYS`). The rest of the environment configuration compared
equal. Runtime provenance and the compatibility helper hash matched the built image.

Instance `57bdb0e4-d146-45bb-90d7-ca6a28097b0d` retained the same PID 1 start identity
through 11 successful health samples over 200 seconds, from 18:35:33 to 18:38:53 UTC.
Logs confirmed shadow Redis readiness and no DNS error, retry-limit shutdown or startup
failure. Both Web replicas then returned login HTML in 562/668 ms and preflight in 316/142 ms,
with all nine dependency checks true. The old failed Plugins deployment is removed.

The other four Node roles then received only the same image and credential-free Valkey URL
changes. Their original `ON_FAILURE` policies and every other configuration field were
preserved. Each replacement passed its own 11 samples over 200 seconds with an unchanged
instance and PID 1 start identity, ending at 18:45:38 UTC. All four previous deployments are
removed, and the production staging area is empty.

| Role | Replacement deployment |
| --- | --- |
| Plugins / CDP | `5f536188-4588-439d-a87e-01fadf363567` |
| Recording API | `919d3591-16b6-4121-b14d-eed1d4ca86d4` |
| Ingestion | `d5497925-4201-4d23-9140-69ed0aadcf19` |
| Recordings Blob Ingestion V2 | `5f27f96d-89d5-477f-91d1-9ccb3c38d4dd` |
| Cyclotron V2 Janitor | `9feda46e-db60-4324-ab45-1c2de990bc0a` |

Every role's actual deployed image and runtime provenance matched the verified `646fbdbd`
artifact. Bounded logs contained no Redis/DNS, retry-limit, startup or shutdown failure.

Chrome's existing authenticated session passed normal Cloudflare verification and opened the
Career Mentor project at the canonical PostHog domain. The preserved dashboard rendered DAU,
WAU, growth, retention and referrer insights. Both synthetic recording entries rendered the
`Uptomic migration canary` DOM. Rewinding the canonical recording visibly advanced the player
from `00:00` to `00:06 / 00:06`; the legacy SDK-host recording also rendered and completed.
No authentication or Cloudflare control was disabled.

The browser also exposed a remaining failure in the preserved `Pageview funnel, by browser`
insight (`dvZh8Vrd`). A fresh retry failed as well. The bounded server diagnostic reported
ClickHouse code 1: its `aggregate_funnel` executable was missing from
`/var/lib/clickhouse/user_scripts`. The production data volume mounts at `/var/lib/clickhouse`
and hides the application's baked-in executable assets. This is a separate release-image
layout defect, not a reason to edit the saved insight or its data. Final migration acceptance
remains open until the mounted-volume-safe image and original funnel pass.

A fresh production ClickHouse volume backup, `fb109df2-c11c-46fa-901f-de71763fa629`, was
created at 18:41:59.976 UTC before the image-layout correction. Railway's backup list verified
it with no expiration. The original pre-migration backup remains retained; no restore or
data-volume replacement was performed.

Release commit `9a5a1e76af20e6c0172dbbc85e6373aaa6042c99` also scopes the existing GitHub
token to upstream resolution/validation, avoiding anonymous API rate limits without expanding
permissions. Its 34 tests and 199 assertions passed, and
[run 33328453625](https://github.com/uptomic/posthog-railway/actions/runs/33328453625)
completed successfully, including all three exact-image checks. Scheduled builds still track
upstream at 02:17 UTC, and production promotion remains a separate verified operation.

### Final Node data-path canary

Run `01a053fd-9f7b-73a2-bfcb-7035a4d3bb95` submitted exactly two synthetic analytics
events and two synthetic replay batches, one through each public hostname. All four requests
returned HTTP 200. An initial canonical replay count was a cached zero; no write was retried.
Fresh `force_blocking` queries at 18:50:46–48 UTC returned HTTP 200, `is_cached=false`, and
count 1 for each of the four records.

Both recordings passed metadata identity checks and actual blob retrieval: HTTP 200, five
JSONL snapshots and 1,148 bytes, containing only the synthetic canary DOM. Their session IDs
are `01a053fd-a212-7285-a447-7688385d7f86` (canonical) and
`01a053fd-aaea-74cb-9911-fa64ae16ee9c` (preserved SDK hostname). Both flag requests returned
valid HTTP 200 responses with no configured flags. MCP initialization, project 1 lookup and
the exact two-event trend aggregate passed in 290, 877 and 491 ms respectively. Subsequent
verification must reuse these IDs read-only; no additional capture is needed for the
ClickHouse image-layout correction.

### Mounted-volume ClickHouse regression

The reviewed correction is release commit `8d7623ad6b48a8b5768566d6e4da882b2f4679ad`.
It relocates unchanged upstream executable assets outside the data volume and pins the
identical engine by digest. Its fingerprint is
`94c5facf01c1619101b826f02b93a5f7f15a9d02e78ba9fb8f2adfa51b81db73`.
All 38 tests and 231 assertions, authenticated lock verification and independent review
passed. The constant-only regression SQL reproduced ClickHouse code 1 against the unchanged
production image; it reads no event tables and changes no stored data.

[Build 33329357124](https://github.com/uptomic/posthog-railway/actions/runs/33329357124)
built the replacement image, but its smoke fixture incorrectly sent a timeout query setting
as the strict read-only dictionary user. ClickHouse correctly rejected that setting with
code 164 before the SQL funnel assertion. The harness now retains that user's server
defaults; permissions and image inputs are unchanged. Independent review and all 38 tests
with 232 assertions passed. A successful exact-image mounted-volume SQL proof and finalized
candidate are still required before promotion. The previous candidate deliberately fails
verification under the new fingerprint contract until then.

### Verified ClickHouse replacement artifact

[Run 33329746666](https://github.com/uptomic/posthog-railway/actions/runs/33329746666)
passed every build, all three exact-image verification jobs, and finalization. At
19:04:06 UTC, the ClickHouse job passed the actual mounted-volume `aggregate_funnel` SQL,
pinned engine/layer provenance, runtime executable access, and cluster/dictionary
authentication checks. The default dictionary user retained its strict read-only settings.

Finalized candidate commit `492d9659f8009df6a790550bbb173e6d1cee2eb9` changes only the
ClickHouse image to
`ghcr.io/uptomic/posthog-railway/clickhouse@sha256:d240f6f0283447b4725aae6e2cc38bd05cbe574d9338666fc44fbf372763e50d`
and records the existing 26.6.2.158 base plus fingerprint. Node `646fbdbd...` and MCP
`f3feb2b3...` remain unchanged. All 38 tests / 232 assertions, authenticated upstream-lock
verification, candidate verification, and read-only Railway plan generation passed again.
The upstream lock remains `da93d8bbd71e23d49a8372ec43b4a4c7f75c1316`; the workflow did
not advance it during the scoped correction.

### Production executable proof and scheduler finding

The minimal production patch changed only ClickHouse's image to `d240f6f0...`.
Deployment `fb0f061d-2e89-4747-be06-09b7bef714bc`, instance
`b3d5ef80-3b07-4918-bf0e-7a3d58d92247`, passed the constant-only funnel proof with
scalar 1. The engine remained 26.6.2.158; all 334 tables, 5,955 columns, and the column
schema fingerprint were unchanged. The runtime executable was visible outside the existing
data mount. An independent registry/layer audit verified identical bytes and executable modes
for all 60 upstream UDF files and no unintended configuration/startup change.

Fresh browser retries of the preserved funnel still failed, and subsequent valid queries
stalled. These browser query IDs were not found in ClickHouse's persisted query log, so the
later scheduling failure must not be attributed to either exact browser query without evidence.
Both Web instances resolved the actual new ClickHouse address, and authentication, `/ping`,
and invalid-SQL parsing remained fast. Valid `SELECT 1` stalled from Web, Worker, and the
local native client. Memory, container PID limits, and CPU quota were not exhausted.

After one controlled same-image ClickHouse-only restart, persisted `system.metric_log`
provided the decisive evidence: every sampled second from 19:12 through 19:25 UTC had
`GlobalThreadActive=512`, the entire configured pool. Scheduled work ranged from 604 to 623.
The CPU scheduler reported no queued CPU work. This proves global worker-pool saturation;
two sleeping pooled UDF processes alone would not have established its cause.

Recovery deployment `5e4f2a12-655a-410f-9d2b-7e9bf6811260`, instance
`ae71be2b-4dc2-4f18-ab8c-ab854ac17dee`, retained the same image, volume and configuration
hash `ffcfb9b3b28988b6fecc935e1caf4dec59ddaf058859566fcac7e397ed834ca2`.
Ten simple queries passed over 90 seconds, 19:30:42–19:32:12 UTC, in 16–46 ms. However,
the baseline already reserved 480 of 512 global workers without user queries. Its 755
container tasks were below the 1,000-task limit; approximately 243 were outside the global pool.

Final acceptance remains open pending a versioned, bounded pool/queue correction, an exact-image
pressure regression, the original saved funnel and dashboard refresh, and fresh read-only
integration checks. No duplicate canary events were captured and no saved insight was edited.
Backup `fb109df2-c11c-46fa-901f-de71763fa629` and earlier rollback anchors remain retained.

### Capacity correction and first pressure run

Commit `579998f8f76cf2a081fa664677f7095f4c01de97` bounds the global pool and
queue at 640 and defaults queries to eight threads, retaining both required background
pools at 128. Independent review, 45 tests / 275 assertions and upstream-lock verification
passed. The image-input fingerprint is
`067cdf9cbe20c1ac91b1c36cd2ce41067a6908889c60d7fbdea9a793ef55bd0d`.

[Run 33332421050](https://github.com/uptomic/posthog-railway/actions/runs/33332421050)
built all images and passed Node, MCP and the ClickHouse mounted-volume SQL checks.
The new pressure gate correctly refused to finalize a candidate: its old-image fixture
calibrated to 480 active workers and recorded six overlapping query starts, zero successful
loads and 11 execution-watchdog failures. Peak container tasks were 906 of 1,000, with no
PID-limit events or OOM kills. However, its persisted metric log contained only baseline
samples, not the required live saturation evidence. A timeout alone is insufficient proof.

The disposable test therefore needs an observer independent of query execution and
metric-log flushing. No production image was promoted by this failed run; the recovered
`d240f6f0...` deployment remains in place. The candidate must still pass the complete
old-image RED/new-image GREEN gate before a ClickHouse-only production change.

The next automatic Pulse sync also succeeded: run
`01a05446-4029-7d10-a6dc-8a4f19fcc611`, completed at 20:04:52.11125 UTC on
`bb256b3519a8dbaddef41b01c705c2a3054a8d0d`. The provider started the hourly
job about four minutes late. The published-report hash and 18-row aggregate hash remain
unchanged; the existing source-freshness warning and historical `not_instrumented`
statuses remain explicit. No scheduling or restart-policy change was made.

The test observer now reads the exact engine's atomic metrics over its disposable private
bridge listener, independently of the SQL watchdog. Missing observations fail closed;
actual monotonic samples, six persisted query intervals, PID ceilings and the existing
RED/GREEN workload remain required. No image input or production listener changed.
Independent source review and 49 tests / 297 assertions, including live local HTTP
sampling while another operation is blocked, passed before the next GitHub run.

### Direct observation proves saturation; pipeline counter correction

[Run 33333048129](https://github.com/uptomic/posthog-railway/actions/runs/33333048129)
at `78d20ef46ada0f958363c9336a877922bcdbd0c2` obtained direct old-image evidence:
73 consecutive saturated observations spanning 36 seconds, active workers 512 with queued
work, six overlapping load queries, zero correct results and 11 watchdog failures. Its
peak task count was 906, without PID-limit events or OOM kills.

The candidate completed all six overlapping queries correctly, with zero watchdog failures,
peak global activity 534, peak tasks 867, and recovery to 480 active/scheduled workers.
The gate nevertheless failed its parallelism assertion because `QueryThread` peaked at six.
Exact engine source shows that counter is incremented around the TCP query handler, while
the pipeline executor owns the separate `QueryPipelineExecutorThreadsActive` counter.
The six-handler observation therefore does not establish a single-threaded workload.
A bounded, read-only `EXPLAIN PIPELINE` of the synthetic SQL on the same production engine
confirmed eight source, filter, expression and aggregation streams; no load was executed.

The observer must measure the actual pipeline-worker counter while retaining the same
minimum parallelism threshold and workload. This failed run did not finalize or promote
the candidate. Production remains on the recovered image pending the corrected full gate.

### Verified capacity artifact

[Run 33333532296](https://github.com/uptomic/posthog-railway/actions/runs/33333532296)
at `2a2350b10cf5f3583544bb164f6f973e272534e8` passed all eight jobs, including
all three immutable-image checks and finalization. Both pressure fixtures confirmed eight
source/filter/aggregation streams before execution. At 20:26:10 UTC, the old image reproduced
36 seconds of queued global-pool saturation, 28 active pipeline workers, six overlapping
stalled queries and 11 watchdog failures. Peak tasks were 909, with no PID-limit or OOM events.

At 20:26:38 UTC, the replacement completed all six overlapping queries correctly, reached
48 active pipeline workers, had no watchdog failures, and recovered to 480 active/scheduled
workers. Peak global activity was 535 of 640 and peak tasks 870 of 1,000, below the required
950 ceiling. No PID-limit events or OOM kills occurred. The mounted-volume UDF proof,
engine/layer provenance and authentication checks also passed.

Finalized manifest `9859e09789ccb5d31d7f1345acb81777c5e06e68` pins ClickHouse to
`ghcr.io/uptomic/posthog-railway/clickhouse@sha256:a554052a426d42dbc8cf9581f4a90af480fb635bc3309ee6f7cc41cb65242805`.
Its fingerprint is `067cdf9cbe20c1ac91b1c36cd2ce41067a6908889c60d7fbdea9a793ef55bd0d`;
the engine, upstream lock, Node and MCP images are unchanged. The authorized production
operation changes only the ClickHouse image, with the `d240f6f0...` image and retained
backups as rollback anchors. Original-funnel/browser and post-restart integration acceptance
remain required before closing the migration.

### Capacity promotion and authenticated application acceptance

At 20:29:23 UTC, the scoped image-only change created ClickHouse deployment
`a8e7dcd4-20d6-4dd9-8168-93e0b78bec9b`, instance
`f8c40a8f-b9b6-4029-ba84-d6fbec7a14a2`. It reached RUNNING at 20:32:14 UTC
on the exact `a554052a...` artifact. The original `/var/lib/clickhouse` volume,
26.6.2.158 engine, 334 tables, 5,955 columns and schema fingerprint
`4336667199921679624` were preserved. No other service configuration changed.

Ten checks over 180 seconds, 20:34:08–20:37:09 UTC, retained the same PID-start
identity. Every execution watchdog passed. Production active/scheduled global workers
remained 508/508, below the 640 limit; container tasks ranged from 886 to 900 of
1,000 with zero PID-limit or OOM events. The production baseline is 508, not the
disposable fixture's calibrated 480. Both Web instances resolved the new private
ClickHouse address and completed authenticated queries in 14–16 ms. A further
20:55:08 check still reported 508 active/scheduled workers and zero active pipeline
workers. The disposable Prometheus observer was not enabled in production.

The preserved `dvZh8Vrd` funnel definition was read and executed unmodified with
`force_blocking`: HTTP 200, uncached results. After refreshing the authenticated
browser, its real UI query completed at 20:39:28 UTC in 138 ms with no exception.
All six original dashboard tiles rendered after the dashboard's Refresh action:
DAU, WAU, growth, retention cohort table, referrers and the three-step browser funnel.
No saved definition was edited. An earlier browser error and periodic Worker
connection errors were not attributed to one another without evidence.

Read-only integration checks reused canary run
`01a053fd-9f7b-73a2-bfcb-7035a4d3bb95`; no additional capture was sent. All four
canonical/old-SDK analytics and replay queries returned HTTP 200, uncached, with
the expected count of one each. Both recording metadata and blob reads returned
HTTP 200 with the expected identity, five synthetic snapshots and 1,148-byte bodies.
Both flag endpoints returned valid empty flag sets. Authenticated MCP initialize,
project retrieval and the canary aggregate query all passed, with aggregate two.

Browser playback was then verified separately. A stale replay-list error cleared
through its Refresh list control; the actual list, metadata and snapshot requests
returned HTTP 200. Canonical recording `01a053fd-a212-7285-a447-7688385d7f86`
and old-SDK recording `01a053fd-aaea-74cb-9911-fa64ae16ee9c` each rendered the
synthetic marker and reached the player's `00:06 / 00:06` end state with a Rewind
control. The old-SDK list row rounds its duration to seven seconds; this receipt
records the actual player display. Temporarily pinned player controls were restored
to their original floating setting. No real user's recording was opened.

Pulse's authenticated dashboard continued to show its finalized August 29 production
snapshot and all 17 governed metric headings. Its typed PostHog probe passed all
four requests. The stored report/digest hash remained
`1fa533581cb5841ce54dfbd531cfe5f91a02545198a503f5b5666ecbe52194ad`, and the
18-row import hash remained
`88874b85d00525de0d3a3e741bd9b2ddc59e7f05687ceb673641f32feb67173c`.
Historical unavailable statuses and the source-freshness warning were preserved.

### Final background-role audit remains open

The final inventory matched all 22 expected immutable images, but found Cyclotron
V2 Janitor crashed. Its final stop was 20:26:01 UTC, before the ClickHouse promotion.
The structured fatal error was a dropped PostgreSQL connection, not an OOM or a
ClickHouse migration failure. Exact upstream source has a raw Janitor pool without
an idle-client error handler, an independently testable failure path. Re-reading the
original fatal row corrected an earlier transcription: the stack names pg 8.10.0,
matching every resolved runtime copy and pg-pool 3.6.0. The asynchronous stack does
not identify the owning pool, so the source correction also requires a real regression
test. The lifecycle's error shutdown exits one. Its effective restart policy is
`ON_FAILURE` with ten retries, not the Plugins
exit-zero case. Provider metadata does not expose the actual exit code or retry count.

Fresh Node database probes succeeded in 34–46 ms against the writable primary and
Patroni's leader/streaming replicas were healthy. A same-artifact Janitor-only
recovery deployment `2aa70e15-ead0-4aa6-a73d-c0fbbb682547` was authorized without
image, configuration or data changes. A narrow idle-pool recovery regression and
durable correction are being evaluated. The migration remains open until this
required background role passes final sustained acceptance.

### Janitor recovery and reviewed regression

The same-image recovery passed eleven health checks over 200 seconds,
20:58:31–21:01:51 UTC, with unchanged PID-start identity `354245607`.
Its cleanup-run counter increased from 34 to 44 between 21:02:28 and 21:04:06 UTC.
The 21:03:41 inventory matched all 22 expected image digests: 18 long-running roles
had 20 RUNNING instances, while four completed migration roles were intentionally
EXITED. No restart policy, schema, data or other service configuration was changed.

Source audit found that the Janitor mode enables the dedicated Janitor only; its
shared PostgreSQL router pools already have error listeners. The dedicated raw
Janitor pool is the unhandled connected pool in that path. This source evidence and
the real regression support the correction, while the asynchronous production stack
alone cannot establish its caller or why the underlying connection was dropped.

Commit `b2620330cd6946c6a00305cc39d13bc12014e38a` adds a guarded, pool-local idle
error listener and a mandatory disposable PostgreSQL test to the existing Node image
verification. Independent review and 56 tests / 323 assertions plus lock verification
passed. A local real-database test using the exact runtime constructor and client
versions reproduced an unhandled idle-disconnect exit one on the original code;
the correction retained the same process, obtained a new backend, preserved an
active query's `57P01` rejection and then queried successfully. This local proof
does not replace the mandatory full-image GitHub test.

Node overlay fingerprint
`d0cccf37a3610e0b2f95b1cea50c9e425b524fa150479b0735d9e95ff4a1b7e9`
retains the official `e526ec11...` base, `49c25324...` revision and `da93d8bb...`
upstream lock. [Build 33335621281](https://github.com/uptomic/posthog-railway/actions/runs/33335621281)
must pass all exact-image gates before this correction is promoted to all five Node
roles. No production fault injection was performed.

Pulse's next automatic sync, `01a0547a-ceef-7c85-a8e3-ab9528f504a0`, succeeded
at 21:02:16.408936 UTC on the unchanged `bb256b35...` deployment. A subsequent
read-only receipt again matched both published-report and 18-row import hashes.
Existing historical limitations were unchanged.
Pulse's production queries accept only the three versioned `identified_*_v1` event
contracts with schema version one and test-account filtering; the migration canary
event name is outside that allowlist.

The exact Node image gate completed at 21:12:21 UTC in job `99322131199`:
the official base reproduced the idle-backend termination and exit one; the
candidate retained its process, obtained a new backend, preserved active-query
rejection and recovered again. Existing IPv6/IPv4 Redis proofs and inherited
startup/provenance checks also passed. Full bundle finalization and production
promotion are still separate required steps.

### Final immutable Node promotion and runtime acceptance

All eight jobs in build `33335621281` passed. Finalized manifest
`f66eb985eb31142aa5f999a10620ffee196613a9` pins Node to
`ghcr.io/uptomic/posthog-railway/node@sha256:a8b98b54245b44463f88b4e5dc8de511d15f265219ebd695192ed91d06e0e943`.
ClickHouse `a554052a...`, MCP `f3feb2b3...`, the official Node base and the upstream
lock are unchanged. Candidate verification and plan generation passed before promotion.

At 21:15:21 UTC, one authorized batch changed only the five Node image fields:

| Role | Deployment |
| --- | --- |
| Plugins / CDP | `3ae6482b-3924-4af0-8c63-b94d0395248a` |
| Ingestion | `bc977de8-4368-4eae-8557-351f31b1465b` |
| Recording blob ingestion | `91c53e71-8cf7-4be6-87d6-cb0c73764941` |
| Recording API | `4664e77a-28cd-47b3-959f-5743e19692c0` |
| Cyclotron V2 Janitor | `2f491499-e88b-4d8c-8506-5e1956560f63` |

Every non-image service field and the rest of the environment matched the pre-change
hashes; no duplicate deployment trigger, migration, volume or restart-policy change
was made. The unaffected environment hash was
`bfee4e62c1c541c9846d0d935cb7d40969456120cdc6e45df3bfe9e88a5d29fe`.

All five services reached RUNNING by 21:16:32 UTC. The final gate completed at
21:20:42 UTC: each service retained its instance and PID-start identity for 200 seconds
and passed eleven health checks. Runtime provenance matched the exact image, base and
overlay fingerprint, including the guarded Janitor handler. Cleanup runs increased
from 9 to 29. No fatal shutdown, Redis retry-limit or Janitor cleanup errors appeared
in the bounded post-start logs. Both Web replicas returned login HTML in 568–601 ms
and preflight in 225–286 ms, with all nine dependencies healthy.

The 21:20:35 final inventory matched all 22 release roles' configured, deployment
and actual image digests: 18 long-running services had 20 RUNNING instances; four
successful one-shot migration roles were intentionally EXITED. Staged changes were empty.

### Worker cache recovery and optional self-hosted integrations

The remaining periodic Worker connection errors belonged to the internal feature-flag
SDK cache provider, not a failed named Celery task. Its polling thread retained a closed
Django connection according to the source/log evidence; this is an inference, not
direct inspection of the original poller's thread-local state. Request/task connection
cleanup does not repair another thread's connection. A fresh database connection alone
was therefore not proof of recovery.

Before the authorized same-image rolling redeploy, targeted inspection confirmed all
expected worker replies, zero active/reserved tasks and one unacknowledged two-second
query-performance poll. Exact Celery/Kombu source confirmed warm shutdown and Redis
redelivery of that normal scheduled message. No task was revoked or cancelled.
Deployment `dae278cf-ca68-405b-b1d4-cb31725ed8f4`, instance
`6bf8f98b-c61c-4fe2-9f12-f66111d880a1`, retained the original `295a24f4...`
image and every configuration field.

After real Celery readiness, eleven checks over 221 seconds retained the same PID
identity and recorded 231 additional task-success signals with zero task failures.
No new closed-connection or HyperCache provider read errors appeared through 21:19 UTC.
The current self-hosted configuration intentionally
has self-capture disabled and no internal PostHog self-team; a fresh provider diagnostic
returns `None` for that absent team, as upstream handles explicitly. This is not a claim
that an internal flag-definition cache became populated. Customer-facing flag endpoints
remain independently verified. The optional AWS SES reputation task also catches missing
credentials and returns normally on self-hosted installations. No AWS integration, team,
flag or secret was created to suppress these expected optional states.

### Final read-only data and browser checks

At 21:24:07–21:24:19 UTC, the final read-only pass on the new Node image returned
all four expected uncached canary counts of one, both identity-matched replay metadata
responses and both five-snapshot / 1,148-byte blobs. Both flag endpoints returned valid
empty sets. Authenticated MCP initialization, project-one retrieval and the exact
two-event aggregate passed. No new captures were sent. A reconstructed helper initially
omitted an explicit UTC timezone and returned zero; correcting both bounds to UTC
returned the expected counts. That validation error was not data loss.

Pulse's actual typed reader passed all four requests in 1,168 / 509 / 377 / 243 ms.
A fresh authenticated Pulse page load rendered the finalized snapshot and all 17 metric
headings with the existing limitations intact. After normal Cloudflare verification,
the original dashboard's Refresh action again rendered all six reports, including the
unchanged funnel and retention table.

That final browser check also isolated an edge issue: an aged browser session's API
requests received Cloudflare challenge HTML with HTTP 403, which the UI displayed as an
insight-permission error. A normal page reload completed automatic security verification
and restored access without changing PostHog permissions. The user subsequently requested
removing this Cloudflare protection for now. That host-scoped change is tracked separately
before final handoff; it does not require changing DNS, TLS or other Uptomic domains.

### PostHog-only Cloudflare challenge disabled

At the user's explicit request, one guarded PATCH at 21:29:44 UTC disabled the
host-exclusive managed-challenge rule for `posthog.uptomic.com`. It did not disable
Cloudflare globally or remove PostHog authentication.

- Zone: `f4f32924f1e9d670c10d30523bc301aa`.
- Ruleset: `6e37ba0d4846407b8bce7df9aebe0887`, version 3 → 4.
- Rule: `fd171a67d8964a90b9a0682c07639b3e`, version 2 → 3.
- Only semantic change: `enabled: true` → `enabled: false`.

The request preserved all existing writable rule fields. A fresh GET confirmed
the action, expression, description and reference were unchanged, as were rule order
and all other rules. The other-rules hash remained
`aa16a2380de8e9a0032215b72ca26b9b27227396ccd3db9d538da17de932fb96`.
The PostHog CNAME, proxied setting, TTL and SSL setting were also unchanged.
No credential, API permission or Career Mentor service configuration changed.

At 21:30:16 UTC, normal HTTPS requests without cookies or authorization returned:

| Request | Result | Duration |
| --- | --- | --- |
| `/login` | 200, PostHog HTML | 547 ms |
| `/health` | 200 | 18 ms |
| `/api/users/@me/` | 401, JSON authentication response | 47 ms |

All three responses had no Cloudflare mitigation header and no challenge HTML.
A subsequent authenticated dashboard reload rendered all six original reports,
including the retention table and the three-step funnel, without a challenge page
or permission error. Replay controls were restored to their original floating mode
after acceptance testing.

The Refresh button remained disabled by the upstream 15-minute bulk-refresh cooldown;
this reload is not represented as another uncached query execution. Exact locked source
in `DashboardReloadAction.tsx`, `dashboardUtils.ts` and `dashboardLogic.tsx` confirms
that condition. The displayed last-refresh age uses the oldest embedded insight,
which can differ from the dashboard-level cooldown clock. The final uncached API
acceptance is recorded separately above.

Rollback is to re-enable this same rule, preserving its other fields and position.
Keep it disabled until the user requests re-enablement; do not create a replacement
zone-wide rule or alter DNS/TLS as a workaround.

### Completion and future releases

This closes the locked-bundle production migration and its runtime, data and browser
acceptance, including the final host-scoped edge change. The daily upstream candidate
workflow remains active at 02:17 UTC; builds verify immutable inputs and compatibility
before finalization. Production promotion remains a separate guarded operation, not
an automatic deployment of an untested upstream commit. Infisical's existing scoped
release identity and secret-sync workflow remain in place; this edge change required
no secret updates.

Production data volumes and recorded backup/restore anchors were retained. The broader
Pulse MVP's separately tracked work and historical metric limitations are not declared
complete by this PostHog migration receipt.
